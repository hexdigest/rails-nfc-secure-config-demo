<form action="/decrypt" method="post">
	<%= hidden_field_tag :authenticity_token, form_authenticity_token %>
	<textarea name="text" rows="5" cols="80" wrap="soft"><%= @text %></textarea>
	<br/>
	<input type="Submit" />
</form>
